import {bootstrap} from "angular2/platform/browser";
import {App} from "./app.component";

/*
This file shows how to bootstrap your application:
 - import the bootstrap function
 - call it on the application-level component you wish to bootstrap
 - note: you have to import that component as well
*/

bootstrap(App);